# Golf AI Project

## プロジェクト概要

ゴルフに関するAI機能を開発するプロジェクト。

## Git 運用ルール

**コードを変更するたびに必ずGitHubにプッシュすること。**

### 手順

1. 変更をステージング
   ```bash
   git add <変更ファイル>
   ```

2. コミット（変更内容を明確に記述）
   ```bash
   git commit -m "変更内容の説明"
   ```

3. GitHubにプッシュ
   ```bash
   git push origin main
   ```

### コミットメッセージ規約

- 新機能追加: `feat: 機能名`
- バグ修正: `fix: 修正内容`
- リファクタリング: `refactor: 変更内容`
- ドキュメント: `docs: 内容`
- テスト: `test: 内容`

### 注意事項

- `.env` ファイルや機密情報は絶対にコミットしない
- 大きな変更は小さな単位に分割してコミットする
- プッシュ前に `git diff` で変更内容を確認する

## 開発環境セットアップ

（プロジェクト開始時に追記）

## ディレクトリ構成

（プロジェクト開始時に追記）
